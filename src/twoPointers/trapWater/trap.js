/**
 * @param {number[]} height
 * @return {number}
 */

export default function trap(height){
    // edge case if the array is <= 2 return 0
    let n = height.length;
    if(n.length <= 2) return 0;
    
}