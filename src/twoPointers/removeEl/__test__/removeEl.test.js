import removeElements from "../removeElements";

describe("remove Elements", () =>{
    it("should return the expected answer with correct length.", () =>{
        expect(removeElements()).toBe
    })
})